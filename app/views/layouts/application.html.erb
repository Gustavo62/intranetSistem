<!DOCTYPE html>
<html id="html_main">
	<head>
		<title>IntraSin</title>
		<meta charset="utf-8"> 
		<meta name="viewport" content="width=device-width,initial-scale=1">
		<%= csrf_meta_tags %>
		<%= csp_meta_tag %>
		<link href='https://fonts.googleapis.com/css?family=Montserrat' rel='stylesheet'> 
		<link rel="stylesheet" type="text/css" href="//fonts.googleapis.com/css?family=Arimo" />
		<link href='https://fonts.googleapis.com/css?family=Poppins' rel='stylesheet'>
		<%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
		<%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %> 
		<%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>   
	</head> 
	<style>
	body{
		font-family: 'Poppins' !important; 
	}
	.profiler-button{
		display:none !important;
	}
	#loading{left: 0px;
    top: 0px;
    position: absolute; 
    height: 100%;
    width: 100%;
    z-index: 1104;
    background-image: url(/assets/loading.gif);
    background-repeat: no-repeat;
    background-position-x: center;
    background-position-y: center;
    background-size: 205px;
    background-color: white;}
	</style>
	<body> 
		<% if signed_in?%>
			<% session.id %>
			<% if user_signed_in?%> 
				<% if current_user.acesso == true%>
					<%= render 'layouts/usuario'%> 
				<% else %> 
					<%= render 'layouts/default'%>
				<% end %>
			<% else %> 
				<% if admin_signed_in?%> 
					<%= render 'layouts/administrador'%>  
				<% else %> 
					<%= render 'layouts/default'%>
				<% end %>
			<% end %>
		<% else %> 
			<%= render 'layouts/default'%>
		<% end %> 
		<%= yield %>
		<script>   
    		document.getElementById("html_main").style.overflow = "auto";
		</script>
		<% if user_signed_in? == false %>
			<footer class="bg-light text-center text-lg-start" style="position: fixed;width: 100%;bottom: 0;z-index:4;">  
				<div class="text-center p-3" style="background-color: rgb(0 0 0 / 0%);"> 
					© 2022 Copyright:
					<a class="text-dark" href="https://mdbootstrap.com/">anoregce.org.br</a>
				</div> 
			</footer> 
		<% end %> 
		<div id="loading"><div>
		<script>  
			$("#loading").hide();  
		</script>
	</body>
</html> 