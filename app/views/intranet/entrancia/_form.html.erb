<%= form_with(model: intranet_entrancium) do |form| %>
  <% if intranet_entrancium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(intranet_entrancium.errors.count, "error") %> prohibited this intranet_entrancium from being saved:</h2>

      <ul>
        <% intranet_entrancium.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
