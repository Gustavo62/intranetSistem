<%= form_with(model: intranet_contribuicao_importada) do |form| %>
  <% if intranet_contribuicao_importada.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(intranet_contribuicao_importada.errors.count, "error") %> prohibited this intranet_contribuicao_importada from being saved:</h2>

      <ul>
        <% intranet_contribuicao_importada.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ano %>
    <%= form.text_field :ano %>
  </div>

  <div class="field">
    <%= form.label :documento %>
    <%= form.text_field :documento %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
