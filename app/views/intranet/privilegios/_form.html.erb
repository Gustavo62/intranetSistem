<%= form_with(model: intranet_privilegio) do |form| %>
  <% if intranet_privilegio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(intranet_privilegio.errors.count, "error") %> prohibited this intranet_privilegio from being saved:</h2>

      <ul>
        <% intranet_privilegio.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div class="field">
    <%= form.label :ativo %>
    <%= form.check_box :ativo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
