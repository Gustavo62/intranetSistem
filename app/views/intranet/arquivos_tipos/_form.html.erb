<%= form_with(model: intranet_arquivos_tipo) do |form| %>
  <% if intranet_arquivos_tipo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(intranet_arquivos_tipo.errors.count, "error") %> prohibited this intranet_arquivos_tipo from being saved:</h2>

      <ul>
        <% intranet_arquivos_tipo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div class="field">
    <%= form.label :ativo %>
    <%= form.check_box :ativo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
